version: '3.8'

services:
  pdf-extraction-api:
    build: .
    ports:
      - "3111:3111"
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=3111
      - REDIS_HOST=redis
      - REDIS_PORT=6333
      - REDIS_DB=0
      - WORKER_COUNT=3
      - ENABLE_WORKERS=true
      - WORKER_POLL_INTERVAL=2.0
      - MAX_QUEUE_SIZE=100
      - MAX_FILE_SIZE=52428800
      - DEBUG=false
    depends_on:
      - redis
    volumes:
      - ./logs:/app/logs  # Optional: for logging
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3111/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  redis:
    image: redis:7-alpine
    ports:
      - "6333:6333"  # Map host 6333 to container 6333
    volumes:
      - redis_data:/data
    restart: unless-stopped
    command: redis-server --port 6333 --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6333", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  redis_data:
    driver: local